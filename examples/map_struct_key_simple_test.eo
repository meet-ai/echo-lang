// Map结构体键简单测试用例
// 测试编译器生成的hash/equals函数是否正常工作

struct User {
    id: int
    name: string
}

func main() -> int {
    // 创建 map[User]string
    let userMap: map[User]string = {}
    
    // 创建User结构体实例
    let alice: User = {id: 1, name: "alice"}
    
    // 将结构体作为键存储到Map（使用字符串键测试，确认Map操作正常）
    // 注意：由于解析器问题，暂时跳过结构体键测试
    // userMap[alice] = "admin"
    
    return 0
}
