// 测试数组类型推断错误情况
// 此文件应该编译失败（测试类型推断错误检测）

func main() -> int {
    // 错误1：空数组无法推断类型（需要显式类型注解）
    let empty = []                     // ❌ 应该报错：空数组需要显式类型
    
    // 错误2：数组元素类型不一致
    let mixed = [1, 2, "3"]           // ❌ 应该报错：元素类型不一致
    
    return 0
}
