// 简单的类型转换运行时测试
// 测试 string.split() 使用新的类型转换

import string

func main() -> int {
    // 测试 string.split() 使用新的类型转换语法
    let s: string = "hello,world,test"
    let parts: []string = s.split(",")
    
    // 返回成功
    return 0
}
