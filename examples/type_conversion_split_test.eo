// 类型转换功能测试 - 使用 print 验证 string.split() 的类型转换
// 测试新实现的类型转换功能

import string

func main() -> int {
    print "=== 类型转换功能测试 ==="
    
    // 测试 string.split() 使用新的类型转换
    print "测试：string.split() 使用新的类型转换语法"
    let s: string = "hello,world,test"
    print "原始字符串: " + s
    
    // 调用 split，这会使用 []string(result) 类型转换
    let parts: []string = s.split(",")
    print "分割完成"
    // 注意：len() 函数调用暂时有问题，先注释掉
    // print "切片长度: " + len(parts)
    
    print "=== 测试完成 ==="
    return 0
}
